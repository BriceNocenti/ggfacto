% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ca.R
\name{correspondence_analysis}
\alias{correspondence_analysis}
\title{Correspondence Analysis of a Crosstab}
\usage{
correspondence_analysis(table, ncp = Inf, ...)
}
\arguments{
\item{table}{A crosstab made with \code{tabxplor::tab()}, with one row variable and one column
variable. A matrix or a \code{table} of counts works too.}

\item{ncp}{The number of axes to keep. All of them by default.}

\item{...}{Additional arguments to pass to \code{\link[FactoMineR]{CA}}.}
}
\value{
A `CA` object from \pkg{FactoMineR}, which remembers the names of the two variables, so
that \code{\link{ca_interpret}} can print them.
}
\description{
Makes the correspondence analysis of a crosstab with
\code{FactoMineR::\link[FactoMineR]{CA}}: first make the table with \code{tabxplor::tab()},
then analyse it. The analysis reads the (weighted) counts of the table, whatever it displays,
without its Total row and column. To leave out some rows or columns, filter the table before, with
\code{dplyr::filter()} and \code{dplyr::select()}.
}
\examples{
tableau <- tabxplor::tab(forcats::gss_cat, race, marital)
res.ca <- correspondence_analysis(tableau)
ca_interpret(res.ca)
ggca(res.ca) |> ggi()
}
